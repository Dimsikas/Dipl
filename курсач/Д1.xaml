<Window x:Class="курсач.Д1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:курсач"
        mc:Ignorable="d"
        Title="Д1" Height="450" Width="800">
    <Grid>

        <Image Source="/Image/RackMultipart20151211-25122-b4va00.jpg" 
               Height="1200" HorizontalAlignment="Center"
               VerticalAlignment="Center" />
        <Border  Background="#7b533a" 
                 Height="60" VerticalAlignment="Top" Opacity="0.6">
            <Label Content="MediaSav" HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   FontSize="52" FontFamily="Brush Script MT" />
        </Border>
        <Border  Background="#7b533a" 
                 Height="434"   Opacity="0.6" Margin="0 60 600 0">
            <Grid>
                <StackPanel>
            <Button Width="200" Height="40" VerticalAlignment="Top" Content="Составление графика" FontSize="20"/>
            <Button Width="200" Height="40" VerticalAlignment="Top" Content="Просмотр заказов" FontSize="20"/>
            <Button Width="200" Height="40" VerticalAlignment="Top" Content="Контроль сотрудников" FontSize="18"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
